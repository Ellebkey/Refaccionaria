<%= form_for(@categprod) do |f| %>
  <% if @categprod.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categprod.errors.count, "error") %> prohibited this categprod from being saved:</h2>

      <ul>
      <% @categprod.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %><br>
    <%= f.text_field :type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
